# PlantDoc_balanced
Balanced version of the PlantDoc dataset for plant disease classification. The original dataset has class imbalance, so minority classes were augmented using image transformations (rotation, flip, shift, zoom) until all classes reached similar sizes. The dataset follows a standard folder structure compatible with TensorFlow and PyTorch.
